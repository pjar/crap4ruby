# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# Ruby
/.bundle/
/coverage/
/pkg/

# CI configuration is tracked in this repo even where a machine-global
# gitignore excludes .github (repo rules take precedence over the global
# excludesfile).
!.github/
